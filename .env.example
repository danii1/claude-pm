# Claude PM Environment Configuration
# Copy this file to .claude-pm/.env and update with your actual values

# JIRA Configuration
# Your JIRA instance URL (without trailing slash)
JIRA_BASE_URL=https://your-org.atlassian.net

# Your JIRA email address
JIRA_EMAIL=your-email@example.com

# Your JIRA API token
# Create one at: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_API_TOKEN=your-api-token

# Default JIRA project key (e.g., PROJ, DEV, TEAM)
JIRA_DEFAULT_PROJECT_KEY=PROJ

# Claude CLI Configuration
# Path to Claude CLI executable (defaults to 'claude' if not specified)
# Common locations: ~/.claude/local/claude or just 'claude' if in PATH
CLAUDE_CLI_PATH=/path/to/claude
